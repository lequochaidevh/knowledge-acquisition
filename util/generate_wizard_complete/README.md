# Build wizard for application (param, flag, ...)

### Workflow:
```sh
touch cli_tree.json # commplete context
source build_and_internal_setup.sh
```

### Test
```sh
app_name <tab-tab-tab> # It will suggest the parameters (like json config file)
```